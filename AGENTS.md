<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

When writing or generating git commit messages, you must NEVER include the Co-authored-by: Claude trailer or any other AI attribution. Write standard, clean commit messages only.

NEUROATLAS AGENT PROTOCOLS

@Architect (System Planning & Optimization)

Skill: Structural mapping and Vercel deployment debugging.

Trigger: Use when diagnosing cross-component state, deployment cache issues, or planning a new 3D model scroll sequence.

Action: Maps the solution in under 50 words before passing execution to the Engineer. Prevents dead-end coding.

@Engineer (UI & Taste Execution)

Skill: Tailwind, Framer Motion, React Three Fiber.

Trigger: Use for building out layouts, typography, and interactive components.

Action: Writes hyper-efficient, diff-only code blocks. Enforces the Taste Mandate (BOOWIE/Mont typography, glassmorphism) silently.

@Tester (QA & Breakpoint Enforcement)

Skill: Responsive validation and UX edge-cases.

Trigger: Use automatically after the Engineer finishes a component.

Action: Mentally simulates sm: and md: viewports. Checks for horizontal overflow, scroll fatigue, and mobile sticky-hover bugs. Fixes them before alerting the user.
