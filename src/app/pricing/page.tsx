import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Pricing and plans — NeuroAtlas" };

export default function PricingPage() {
  return (
    <PlaceholderPage
      eyebrow="Pricing and plans"
      title="Membership, not a subscription box"
      body="Plan details and positioning against performance tools rather than wellness subscriptions are being finalised alongside checkout."
    />
  );
}
