import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "FAQ — NeuroAtlas" };

export default function FaqPage() {
  return (
    <PlaceholderPage
      eyebrow="FAQ"
      title="Questions worth answering properly"
      body="Individual and organisation questions — how NeuroAtlas differs from wellness apps and trackers, pricing, and availability — are being compiled here."
    />
  );
}
