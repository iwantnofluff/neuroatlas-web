import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Toolkits — NeuroAtlas" };

export default function ToolkitsPage() {
  return (
    <PlaceholderPage
      eyebrow="Toolkits"
      title="Built for how pressure moves through your day"
      body="Wellness, stress and performance toolkits, each with session lengths, when to use them, and the science behind them, are being catalogued here."
    />
  );
}
